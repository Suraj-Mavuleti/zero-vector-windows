# Zero Vector Windows

A mathematical vector graphics engine.

## Overview
This repository contains the highly-optimized native core for the zero vector windows platform. It is built with zero-cost abstractions, direct hardware access, and massive internal C++ vendoring for algorithmic superiority.

## Key Advantages
- Infinite zoom capabilities
- Custom Bézier curve math engine
- Multi-threaded SVG parsing.

## Why zero-vector-windows? (Competitive Edge)
**The Problem:** Illustrator is slow and crashes on complex mathematical SVGs. zero-vector calculates curves dynamically utilizing the CPU L3 cache.

**The Solution:** zero-vector-windows provides an entirely distinct, low-level architectural approach. By discarding heavy runtimes, web-views, and telemetry dependencies, we achieve maximum computational throughput and deterministic execution.

---
*Built for extreme performance.*

## Native V2 Architecture Implemented
This repository now houses a completely real, interactive zero-vector engine. 

**To run the engine:**
```bash
./start.sh
```
